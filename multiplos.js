let num = 100; // Número que se evaluara 

while (num >= 0) {
    if (num % 3 === 0) { // Evalua si es divisible por 3
        console.log(num + ' ' + "divisible por 3") // Imprime el número que es divisible
    }
    num--;// Va restando el número
}